[package]
name = "raspi"
version = "0.1.0"
edition = "2021"

[features]
raspi3 = []
raspi4 = []

[dependencies]
common = { path = "../../common" }
bitfield = "=0.14.0"
tock-registers = "0.9.0"
aarch64-cpu = "=9.4.0"

[dependencies.fdt-rs]
version = "0.4.5"
default-features = false
